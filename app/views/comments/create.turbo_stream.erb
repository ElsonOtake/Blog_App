<%= turbo_stream.prepend "comments_#{@post.id}", partial: "comments/comment", locals: { comment: @comment } %>
<%= turbo_stream.update "new_comment_#{@post.id}", "" %>
<%= render_turbo_stream_flash_messages %>
