<%= turbo_frame_tag :posts do %>
  <%= render @posts %>
<% end %>

<% if @pagy.next.present? %>
  <%= turbo_frame_tag :pagination,
    loading: :lazy,
    src: member_posts_path(@member, format: :turbo_stream, page: @pagy.next)
  %>
<% end %>
